# VoucherVision vendor (no Streamlit removal — upstream list)
-r VoucherVision/requirements.txt

# API layer
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
gunicorn>=23.0.0
python-multipart>=0.0.9
pydantic>=2.8.0
pydantic-settings>=2.2.0
jinja2>=3.1.0

# PyTorch: >=2.6 required — torch.load change (CVE-2025-32434). Retest LM2/VoucherVision if regressions.
torch>=2.6.0
