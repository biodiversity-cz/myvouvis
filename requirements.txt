# VoucherVision vendor (no Streamlit removal — upstream list)
-r VoucherVision/requirements.txt

# API layer
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
gunicorn>=23.0.0
python-multipart>=0.0.9
pydantic>=2.8.0
pydantic-settings>=2.2.0
jinja2>=3.1.0

# Upstream / README: PyTorch < 2.6 for some LM2 / LeafMachine2 workflows
torch>=2.0.0,<2.6.0
